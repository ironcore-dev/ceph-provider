apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # IMPORTANT: this ref pins the volumepoollet RBAC and MUST match the
  # ironcore-volumepoollet image version you deploy  Bump both together,
  # a newer volumepoollet may need permissions this ref doesn't grant.
  # TODO: drive both from a single version to remove the manual sync.
- github.com/ironcore-dev/ironcore/config/volumepoollet-broker/poollet-rbac?ref=v0.6.1
- leader_election_role.yaml
- leader_election_role_binding.yaml
